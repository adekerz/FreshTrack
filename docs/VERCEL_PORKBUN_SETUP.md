# Настройка Vercel и Porkbun

Руководство по настройке фронтенда на Vercel и подключению домена через Porkbun.

## 🚀 Vercel - Деплой фронтенда

### 1. Подключение проекта к Vercel

1. Зайдите на [vercel.com](https://vercel.com)
2. Импортируйте ваш GitHub репозиторий
3. Vercel автоматически определит Vite проект

### 2. Переменные окружения в Vercel

В настройках проекта Vercel → **Settings** → **Environment Variables** добавьте:

```bash
# URL API (бэкенд на Railway)
VITE_API_URL=https://api.freshtrack.systems/api
```

**Важно:** 
- Переменные с префиксом `VITE_` доступны в клиентском коде
- После добавления переменных нужно пересобрать проект

### 3. Настройка домена в Vercel

1. Перейдите в **Settings** → **Domains**
2. Добавьте ваш домен: `freshtrack.systems`
3. Vercel покажет DNS записи, которые нужно добавить в Porkbun

## 🌐 Porkbun - Настройка DNS записей

### DNS записи для Vercel (фронтенд)

В панели управления Porkbun добавьте следующие DNS записи:

#### Вариант 1: Использование CNAME (рекомендуется)

```
Type: CNAME
Name: @ (или оставьте пустым для корневого домена)
Value: cname.vercel-dns.com
TTL: 3600
```

**Примечание:** Если Porkbun не поддерживает CNAME для корневого домена (@), используйте A записи (см. Вариант 2).

#### Вариант 2: Использование A записей (для корневого домена)

Vercel предоставит IP адреса в настройках домена. Добавьте A записи:

```
Type: A
Name: @
Value: [IP адрес от Vercel]
TTL: 3600
```

Повторите для всех IP адресов, которые предоставит Vercel (обычно 2-4 адреса).

#### Для поддомена www

```
Type: CNAME
Name: www
Value: cname.vercel-dns.com
TTL: 3600
```

### DNS записи для Railway (бэкенд API)

Для поддомена `api.freshtrack.systems`:

1. В Railway найдите ваш проект
2. Перейдите в **Settings** → **Networking**
3. Добавьте кастомный домен: `api.freshtrack.systems`
4. Railway предоставит CNAME запись

В Porkbun добавьте:

```
Type: CNAME
Name: api
Value: [CNAME значение от Railway]
TTL: 3600
```

**Пример значения от Railway:**
```
xxxxx.railway.app
```

### Полный список DNS записей для Porkbun

```
# Фронтенд (Vercel)
Type: A (или CNAME)
Name: @
Value: [IP от Vercel] или cname.vercel-dns.com
TTL: 3600

Type: CNAME
Name: www
Value: cname.vercel-dns.com
TTL: 3600

# Бэкенд API (Railway)
Type: CNAME
Name: api
Value: [CNAME от Railway]
TTL: 3600
```

## 📋 Пошаговая инструкция

### Шаг 1: Настройка Vercel

1. ✅ Импортируйте репозиторий в Vercel
2. ✅ Добавьте переменную окружения `VITE_API_URL=https://api.freshtrack.systems/api`
3. ✅ Дождитесь успешного деплоя
4. ✅ Перейдите в **Settings** → **Domains**
5. ✅ Добавьте домен `freshtrack.systems`
6. ✅ Скопируйте DNS записи, которые покажет Vercel

### Шаг 2: Настройка DNS в Porkbun

1. ✅ Войдите в панель управления Porkbun
2. ✅ Выберите домен `freshtrack.systems`
3. ✅ Перейдите в раздел **DNS Records**
4. ✅ Добавьте DNS записи для Vercel (из шага 1)
5. ✅ Добавьте CNAME запись для `api` поддомена (Railway)

### Шаг 3: Настройка Railway

1. ✅ В Railway перейдите в **Settings** → **Networking**
2. ✅ Добавьте кастомный домен: `api.freshtrack.systems`
3. ✅ Скопируйте CNAME значение
4. ✅ Добавьте это значение в Porkbun для поддомена `api`

### Шаг 4: Проверка

После настройки DNS (может занять до 48 часов, обычно 5-30 минут):

1. ✅ Проверьте `https://freshtrack.systems` - должен открываться фронтенд
2. ✅ Проверьте `https://api.freshtrack.systems/api/health` - должен отвечать бэкенд
3. ✅ Проверьте работу приложения

## 🔧 Проверка DNS записей

Используйте команду для проверки DNS:

```bash
# Проверка A записи для корневого домена
dig freshtrack.systems A

# Проверка CNAME для www
dig www.freshtrack.systems CNAME

# Проверка CNAME для api
dig api.freshtrack.systems CNAME
```

Или используйте онлайн инструменты:
- [whatsmydns.net](https://www.whatsmydns.net)
- [dnschecker.org](https://dnschecker.org)

## ⚠️ Важные замечания

### SSL сертификаты

- **Vercel**: Автоматически выдает SSL сертификаты через Let's Encrypt
- **Railway**: Автоматически выдает SSL сертификаты
- **Porkbun**: Не нужно настраивать SSL вручную, все автоматически

### Время распространения DNS

- DNS изменения могут занять от 5 минут до 48 часов
- Обычно изменения применяются в течение 30-60 минут
- Используйте `dig` или онлайн инструменты для проверки

### CNAME для корневого домена

- Некоторые регистраторы (включая Porkbun) поддерживают CNAME для корневого домена через ALIAS/ANAME записи
- Если CNAME не поддерживается, используйте A записи с IP адресами от Vercel

### Поддомен api

- Убедитесь, что в Railway правильно настроен кастомный домен
- Проверьте, что CNAME запись в Porkbun указывает на правильный домен Railway

## 🐛 Решение проблем

### Домен не работает после настройки

1. Проверьте DNS записи через `dig` или онлайн инструменты
2. Убедитесь, что прошло достаточно времени (минимум 5-10 минут)
3. Очистите DNS кэш: `ipconfig /flushdns` (Windows) или `sudo dscacheutil -flushcache` (Mac)

### API не доступен

1. Проверьте, что в Railway добавлен кастомный домен `api.freshtrack.systems`
2. Проверьте CNAME запись в Porkbun
3. Убедитесь, что Railway проект запущен и работает

### CORS ошибки

1. Проверьте переменную `CORS_ORIGINS` в Railway
2. Убедитесь, что `https://freshtrack.systems` добавлен в `CORS_ORIGINS`
3. Перезапустите Railway проект после изменения переменных

## 📝 Конфигурация vercel.json

Ваш `vercel.json` уже настроен правильно:

```json
{
  "framework": "vite",
  "buildCommand": "vite build",
  "outputDirectory": "dist",
  "installCommand": "npm install --include=dev",
  "env": {
    "VITE_API_URL": "https://api.freshtrack.systems/api"
  },
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
```

Эта конфигурация:
- ✅ Правильно собирает Vite проект
- ✅ Настраивает SPA роутинг (rewrites)
- ✅ Устанавливает переменную окружения для API

## 🔐 Безопасность

### Рекомендации

1. ✅ Используйте HTTPS везде (Vercel и Railway автоматически)
2. ✅ Не коммитьте секретные ключи в репозиторий
3. ✅ Используйте переменные окружения для всех конфиденциальных данных
4. ✅ Регулярно обновляйте зависимости

### Переменные окружения

**Vercel (публичные, доступны в клиенте):**
- `VITE_API_URL` - URL API (публичный)

**Railway (приватные, только на сервере):**
- `JWT_SECRET` - секретный ключ
- `RESEND_API_KEY` - ключ API
- `DATABASE_URL` - строка подключения к БД
- И другие секретные переменные
