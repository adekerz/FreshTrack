# Resend Webhooks & System Email Notifications

## Обзор

Реализована полная интеграция Resend для системных email-уведомлений:
- Webhook endpoint для обработки событий доставки/отказов/жалоб
- Email-уведомления о скором истечении срока годности
- Ежедневные системные отчеты по email

## Архитектура

### 1. Webhook Endpoint

**POST /webhooks/resend**

Обрабатывает события от Resend:
- `email.delivered` - логирование доставки
- `email.bounced` - пометка email как invalid (`email_valid = false`)
- `email.complained` - блокировка email (`email_blocked = true`)

**Безопасность:**
- Всегда возвращает 200 (Resend не будет ретраить)
- Ошибки логируются, но не ломают систему
- Неизвестные события игнорируются

### 2. Email-уведомления о истечении

**Триггер:** Ежедневная проверка (вместе с daily report)

**Логика:**
- Группировка товаров по отделам
- Одно письмо на отдел
- Получатели: админы отеля и менеджеры отдела (с валидными email)
- Фильтрация по `email_valid` и `email_blocked`

**Шаблон:** `expiryWarningTemplate`
- Список товаров с датами истечения
- Количество дней до истечения
- CTA: "Открыть FreshTrack"

### 3. Ежедневные системные отчеты

**Триггер:** Ежедневно в настроенное время (из правил уведомлений)

**Содержимое:**
- Всего активных партий
- Партий с истекающим сроком
- Просроченных партий
- Списаний за сутки

**Получатель:** `system@freshtrack.systems` (по умолчанию)

**Шаблон:** `dailyReportTemplate`
- Таблица со статистикой
- CTA: "Открыть инвентарь"

## База данных

### Миграция: `031_email_status_tracking.sql`

Добавлены поля в таблицу `users`:
- `email_valid BOOLEAN DEFAULT TRUE` - валидность email (false если bounced)
- `email_blocked BOOLEAN DEFAULT FALSE` - блокировка (true если complained)

**Индекс:** `idx_users_email_status` для быстрых запросов

## Email Service

### Новые функции

1. **`sendExpiryWarningEmail({ products, department, hotel, to })`**
   - Отправка уведомлений о истекающих товарах
   - Группировка по отделам

2. **`sendDailyReportEmail({ stats, to })`**
   - Отправка ежедневных отчетов
   - По умолчанию: `system@freshtrack.systems`

### Шаблоны

- `systemEmailLayout()` - базовый layout для системных писем
- `expiryWarningTemplate()` - шаблон для expiry warnings
- `dailyReportTemplate()` - шаблон для daily reports

## NotificationEngine

### Новые методы

1. **`sendExpiryWarningEmails()`**
   - Получает активные правила с email-каналом
   - Группирует товары по отделам
   - Отправляет email для каждого отдела

2. **`sendDailyReports()` (расширен)**
   - Теперь отправляет и Telegram, и Email отчеты
   - Email-отчеты отправляются для каждого отеля

### Фильтрация получателей

Все запросы пользователей для email-уведомлений фильтруются:
```sql
WHERE email IS NOT NULL 
  AND (email_valid IS NOT FALSE AND email_blocked IS NOT TRUE)
```

## Notification Jobs

### Daily Report Job

Выполняется в настроенное время (из правил уведомлений):

1. Проверка истекающих партий (`checkExpiringBatches`)
2. **Отправка email-уведомлений о истечении** (`sendExpiryWarningEmails`)
3. Обработка очереди (`processQueue`)
4. Ежедневные отчеты (`sendDailyReports` - Telegram + Email)

## Безопасность и стабильность

- ✅ Webhook endpoint не падает при ошибках
- ✅ Email-ошибки не ломают основной процесс
- ✅ Все ошибки логируются
- ✅ Email - secondary канал (не блокирует UI)
- ✅ Фильтрация невалидных/заблокированных email

## Конфигурация

### Environment Variables

```env
EMAIL_PROVIDER=resend
RESEND_API_KEY=re_xxxxxxxxxxxx
APP_URL=https://freshtrack.systems
```

### Webhook URL для Resend

```
https://your-domain.com/webhooks/resend
```

Настроить в Resend Dashboard → Webhooks

## Тестирование

### Webhook

```bash
curl -X POST http://localhost:3001/webhooks/resend \
  -H "Content-Type: application/json" \
  -d '{
    "type": "email.bounced",
    "data": {
      "email": "test@example.com",
      "bounce_type": "hard",
      "reason": "Invalid recipient"
    }
  }'
```

### Email отправка

Ручной триггер через API (если нужно):
- Daily report: `NotificationEngine.sendDailyReports()`
- Expiry warnings: `NotificationEngine.sendExpiryWarningEmails()`

## Мониторинг

Все операции логируются:
- `📧` - Email отправка
- `📬` - Webhook события
- `❌` - Ошибки
- `⚠️` - Предупреждения

Проверка статуса email пользователей:
```sql
SELECT email, email_valid, email_blocked 
FROM users 
WHERE email IS NOT NULL;
```
