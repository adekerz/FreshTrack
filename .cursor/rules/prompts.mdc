---
applyTo: '**'
---

# FreshTrack Cursor Prompts

## 🔹 ОБЩЕЕ ПРАВИЛО

Отвечай строго по правилам FreshTrack Cursor Rules.
- Язык — русский
- Только production-код
- Backend — source of truth
- Если есть сомнение — сначала задай вопрос

---

## 🧠 ШАБЛОНЫ PROMPTS

### 1️⃣ Backend: новый API endpoint

```
Реализуй backend endpoint для: <ОПИСАНИЕ ФИЧИ>.

Требования:
- Node.js + Express
- Валидация входных данных (zod)
- Permission check через requirePermission(resource, action)
- Вся логика в service
- Изоляция данных по hotel_id
- Audit logging для всех мутаций
- Чёткие ошибки без stack trace

Покажи:
1. Route
2. Validation schema
3. Service
4. Audit
5. Пример ответа API

use context7
```

---

### 2️⃣ Backend: доработка существующего endpoint

```
Проанализируй существующий backend код и доработай его.

Цель: <ЧТО ДОЛЖНО ИЗМЕНИТЬСЯ>

Проверь и исправь:
- permissions
- отсутствие audit
- отсутствие hotel_id фильтра
- бизнес-логику в route
- неочевидные edge cases

НЕ меняй архитектуру.
НЕ добавляй новые таблицы.
НЕ добавляй лишние фичи.

Покажи только исправленный код и чеклист проверки.
use context7
```

---

### 3️⃣ Frontend: новая форма / страница

```
Реализуй frontend UI для: <ОПИСАНИЕ ФИЧИ>.

Требования:
- React / Next.js
- Без бизнес-логики
- Все данные только с backend API
- Loading / Success / Error states
- Mobile-first (touch ≥ 48px)
- Доступна из навигации
- Permissions учитываются (через permissions из API)

Backend уже существует — ничего не вычисляй на фронте.

Покажи:
1. Компонент
2. API вызов
3. UX states
4. Интеграцию в страницу

use context7
```

---

### 4️⃣ Frontend: проверить UI ↔ Backend mismatch

```
Проведи аудит Frontend ↔ Backend.

Найди:
- UI элементы без backend endpoint
- Backend endpoints без UI
- Несовпадение полей API и UI
- Hardcoded статусы / цвета / роли
- Логику, которая должна быть на бэке

Выведи результат в таблице:
| UI | Backend | Проблема | Что исправить |

Код не пиши, только анализ.
```

---

### 5️⃣ Full-stack: реализация фичи end-to-end

```
Реализуй фичу end-to-end: <ОПИСАНИЕ>.

Обязательные этапы:
1. Анализ (permissions, UX, интеграция)
2. Backend: route → validation → permission → service → audit
3. Frontend: форма → API → states → навигация
4. Edge cases
5. Чеклист тестирования

НЕ останавливайся на половине.
Функция должна быть реально используема.

use context7
```

---

### 6️⃣ Code Review (строгий, enterprise)

```
Проведи строгий code review.

Проверь:
- Security (RBAC, permissions)
- Изоляцию hotel_id
- Backend как source of truth
- Отсутствие логики в UI
- Production readiness

Ответь в формате:
1. Критические ошибки (BLOCKER)
2. Важные проблемы (MAJOR)
3. Косметика (MINOR)
4. Итог: готово / не готово

Код не переписывай, только анализ.
```

---

### 7️⃣ Реалтайм (SSE / WebSocket)

```
Реализуй realtime обновление для: <ЧТО ОБНОВЛЯЕТСЯ>.

Требования:
- Backend отправляет события
- Frontend подписывается
- Без перезагрузки страницы
- Auth обязателен
- Без лишней логики на фронте

Покажи:
- SSE endpoint
- Тип события
- Подписку на фронте

use context7
```

---

## 📋 БЫСТРЫЕ КОМАНДЫ

| Задача | Промпт |
|--------|--------|
| Новый endpoint | `backend endpoint: <описание>` |
| Новая форма | `frontend form: <описание>` |
| Фича целиком | `full-stack: <описание>` |
| Code review | `review: <файл или фича>` |
| Аудит UI↔API | `audit: <модуль>` |
| SSE/Realtime | `realtime: <что обновлять>` |
